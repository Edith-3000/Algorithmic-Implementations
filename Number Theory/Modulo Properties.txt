• (a + b) % m = ((a % m) + (b % m)) % m
• (a - b) % m = ((a % m) - (b % m) + m) % m
• (a * b) % m = ((a % m) * (b % m)) % m
• (a / b) % m = ((a % m) * (b⁻¹ % m)) % m, where b⁻¹ = Multiplicative modulo inverse of b under modulo m